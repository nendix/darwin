# Darwin

Simulazione Predator/Prey con Algoritmi Genetici, interfaccia 2D con pygame.

## Setup
Usa il tuo Makefile (fornito separatamente).

Esempio:
```bash
make venv
source .venv/bin/activate
make deps
make run
